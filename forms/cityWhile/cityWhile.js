/*
cityWhile = []
let keepgoing = true

while (keepgoing == true) {
  moreCity = prompt('so you want to enter a city?')
  if (moreCity == "yes") {
  city = prompt('enter a city')
  cityWhile.push(city)
}
  else {
  keepgoing = false
  }
  }
let count = 0
while (cityWhile.length !== count) {
  let cities = cityWhile[count]
  console.log(cities.toLowerCase())
  count++
}

*/